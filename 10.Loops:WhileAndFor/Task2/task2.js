// For every loop iteration, 
// write down which value it outputs and then compare it with the solution.
// Both loops alert the same values, or not?

// The prefix form ++i:
let i = 0;
while (++i < 5) alert( i );
//1,2,3,4, end loop.

//The postfix form i++
let j = 0;
while (j++ < 5) alert( j );
//1,2,3,4 end loop.

//Both loops alert the same values